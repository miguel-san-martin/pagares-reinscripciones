import { FormatoamdhhFormatodmaPipe } from './formatoamdhh-formatodma.pipe';

describe('FormatoamdhhFormatodmaPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatoamdhhFormatodmaPipe();
    expect(pipe).toBeTruthy();
  });
});
