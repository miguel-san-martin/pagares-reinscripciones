/**
 *  Respuesta que llegua del backend con la lista de fechas
 *
 * @export
 * @interface ConsultaFecha
 */
export interface ConsultaFecha {
  idPlazoDetalle:   string;
  FechaVencimiento: string;
  idPlazoSet:       string;
}
