/**
 * Si no se especifica otro, los valores especificados aquí se usarán en
 * al momento de la construcción.
 */
export const environment = {
  /**
   * Bandera para indicar si el entorno es un ambiente de producción o no.
   */
  production: true,
  /**
   * URL del servidor donde se está ejecutando el proyecto.
   */
  server: 'https://sie.iest.edu.mx',
  /**
   * URL absoluta del proyecto incluyendo subdirectorios.
   */
  url: 'https://sie.iest.edu.mx/app/contraloria/reinscripciones/generaciondePagares/',
  /**
   * Nombre del proyecto.
   */
  project: 'recibos-caja',
};
